# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           golang 1.0

go.setup            github.com/tomasz-tomczyk/crit 0.18.0 v
go.offline_build    no
github.tarball_from archive
revision            0

description         A feedback tool for AI agents to review plans, diffs and frontends

long_description    Crit provides an adaptive UI for reviewing the output of AI \
                    agents, whether that be plans, code diffs, running web \
                    applications or static HTML files. You can leave inline \
                    comments that the agents can then address.

homepage            https://github.com/tomasz-tomczyk/crit

categories          devel
installs_libs       no
license             MIT
maintainers         nomaintainer

platforms           darwin
supported_archs     x86_64 arm64

checksums           rmd160  3353790c43558724faace986fecfb36b4e5d0a94 \
                    sha256  29e7d262282f7e6a7b4efbf4d0b43a738333370d07df67f6f5be944752f4be6d \
                    size    5416487

build.pre_args-append \
    -ldflags \"-s -w -X main.version=${version}\"
build.args          ./cmd/${name}

destroot {
    xinstall -m 0755 ${worksrcpath}/${name} ${destroot}${prefix}/bin/
}
